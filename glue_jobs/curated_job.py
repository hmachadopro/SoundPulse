"""Job Glue (PySpark) staging → curated : modélisation en étoile.

TODO (phase 3) : construire `fait_popularite`, `dim_titre`, `dim_artiste`,
`dim_date` à partir des Parquet `staging/`, écrire dans `curated/`.
"""
raise NotImplementedError("curated_job : à implémenter en phase 3")
